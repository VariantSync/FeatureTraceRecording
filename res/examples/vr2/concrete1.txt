
void main(String[] args) {
    runGame();
}

== insert under context Setup ==> // "setup(args);" is now mapped to Setup

void main(String[] args) {
    bool success = setup(args);
    runGame();
}

== insert under context Setup ==> // condition is mapped to Setup

void main(String[] args) {
    bool success = setup(args);
    if (success) {

    }
    runGame();
}

== move under context null ==> // runGame is still mapped to Setup

void main(String[] args) {
    bool success = setup(args);
    if (success) {
        runGame();
    }
}

== delete under context VR ==> // "bool success = setup(args);" is now mapped to "Setup AND (NOT VR)"
void main(String[] args) {
    if (success) {
        runGame();
    }
}

== insert under context VR ==> // "vr_setup" is mapped to VR

void main(String[] args) {
    bool success = vr_setup();
    if (success) {
        runGame();
    }
}

// now we have the following presence conditions
void main(String[] args) { // null
    vr_setup(); // VR
    if (setupSuccessful) { // Setup
        runGame(); // null
    }
}
